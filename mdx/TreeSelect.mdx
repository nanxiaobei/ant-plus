---
name: TreeSelect 树选择
route: /tree-select
---

import { Playground, PropsTable } from 'docz';
import { Form, TreeSelect } from '../src';

# TreeSelect 树选择

## 使用

```js
import { TreeSelect } from 'antx';
```

## 示例

<Playground>
  <TreeSelect
    data={[
      {
        id: 'tiger',
        name: '🐅 小脑斧',
        children: [{ id: 'little tiger', name: '🐅 小脑斧的儿子' }],
      },
      { id: 'dog', name: '🐶 小狗子', children: [{ id: 'little dog', name: '🐶 小狗子的儿子' }] },
      { id: 'cat', name: '🐱 小猫崽' },
    ]}
    keys={['id', 'name']}
    onChange={(value, selectedOptions) => {
      console.log('value: ', value);
      console.log('selectedOptions: ', selectedOptions);
    }}
    search
    check
    msg="选个宠物吧"
    searchMsg="找一找"
  />
</Playground>

## API

其它 Props 与 [Ant Design TreeSelect](https://ant.design/components/tree-select-cn/) 完全一致，以下为增强 Props。

<PropsTable of={TreeSelect} />
